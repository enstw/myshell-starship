"$schema" = 'https://starship.rs/config-schema.json'

# ─────────────────────────────────────────────────────────────
# Kali-style two-line framed prompt, ANSI colors that adapt to
# the terminal theme (like `ssh airbug`):
#
#   ┌──(user<os>host)-[~/dir]-[ git ]-[ langs ]-[ time ]   <os> = OS glyph connector
#   └─$
#
# Same information as before (os/host/user, dir, git branch/
# status/state, all languages, package, cmd_duration, time),
# just re-framed in Kali's box style instead of powerline bg.
# Each -[…] group is wrapped in (…) so it vanishes when empty.
# ─────────────────────────────────────────────────────────────

format = """
[┌──](bold green)\
$username\
$os\
$hostname\
$directory\
$git_branch\
$git_status\
$git_state\
$nodejs\
$rust\
$golang\
$php\
$python\
$java\
$ruby\
$docker_context\
$package\
$cmd_duration\
$time
[└─](bold green)$character"""

# ─── Prompt Character (Kali $ / #) ───────────────────────────

[character]
success_symbol = '[\$](bold blue)'
error_symbol = '[\$](bold red)'

# ─── Identity: ( user<os>host ) — OS glyph joins user & host ─

[os]
disabled = false
format = '[$symbol](bold blue)'

[username]
show_always = true
style_user = "bold blue"
style_root = "bold red"
format = '[\(](bold green)[$user]($style)'

[hostname]
ssh_only = false
format = '[$hostname](bold blue)[\)](bold green)'

# ─── Directory: -[ ~/dir ] ───────────────────────────────────

[directory]
style = "bold"
read_only_style = "bold red"
format = '[-\[](bold green)[$path]($style)[$read_only]($read_only_style)[\]](bold green)'
truncation_length = 3
truncation_symbol = "…/"
home_symbol = "󰋜 "
read_only = " 󰌾"

[directory.substitutions]
"Documents" = "󰈙 "
"Downloads" = " "
"Music" = " "
"Pictures" = " "

# ─── Git (Chinese TC labels) ─────────────────────────────────

[git_branch]
symbol = " "
format = '([-\[](bold green)[位於 $symbol$branch](bold purple)[\]](bold green))'

[git_commit]
tag_symbol = "  "

[git_status]
format = '([-\[](bold green)[$all_status$ahead_behind](bold red)[\]](bold green))'
conflicted = "衝突"
ahead = "領先"
behind = "落後"
modified = "修改"
staged = "暫存"
untracked = "未追蹤"
deleted = "刪除"
renamed = "重新命名"
stashed = "貯存"

[git_state]
format = '([-\[](bold green)[$state($progress_current/$progress_total)](bold yellow)[\]](bold green))'
rebase = "重定基底"
merge = "合併"
revert = "還原"
cherry_pick = "揀選"
bisect = "二分搜尋"
am = "am"
am_or_rebase = "am/重定基底"

# ─── Other VCS (Chinese TC) ──────────────────────────────────

[fossil_branch]
symbol = " "
format = '([-\[](bold green)[位於 $symbol$branch](bold purple)[\]](bold green))'

[hg_branch]
symbol = " "
format = '([-\[](bold green)[位於 $symbol$branch](bold purple)[\]](bold green))'

# ─── Languages ───────────────────────────────────────────────

[nodejs]
symbol = " "
format = '([-\[](bold green)[$symbol$version](bold cyan)[\]](bold green))'

[rust]
symbol = "󱘗 "
format = '([-\[](bold green)[$symbol$version](bold cyan)[\]](bold green))'

[golang]
symbol = " "
format = '([-\[](bold green)[$symbol$version](bold cyan)[\]](bold green))'

[php]
symbol = " "
format = '([-\[](bold green)[$symbol$version](bold cyan)[\]](bold green))'

[python]
symbol = " "
format = '([-\[](bold green)[$symbol$version(\($virtualenv\))](bold cyan)[\]](bold green))'

[java]
symbol = " "
format = '([-\[](bold green)[$symbol$version](bold cyan)[\]](bold green))'

[ruby]
symbol = " "
format = '([-\[](bold green)[$symbol$version](bold cyan)[\]](bold green))'

[docker_context]
symbol = " "
format = '([-\[](bold green)[$symbol$context](bold cyan)[\]](bold green))'

# ─── Package (Chinese TC) ────────────────────────────────────

[package]
symbol = "󰏗 "
format = '([-\[](bold green)[為 $symbol$version](bold yellow)[\]](bold green))'

# ─── Duration & Time (Chinese TC) ────────────────────────────

[cmd_duration]
format = '([-\[](bold green)[歷 $duration](bold yellow)[\]](bold green))'

[time]
disabled = false
time_format = "%R"
format = '[-\[](bold green)[ $time](bold white)[\]](bold green)'

# ─── OS symbols (auto-detected) ──────────────────────────────

[os.symbols]
Alpaquita = " "
Alpine = " "
AlmaLinux = " "
Amazon = " "
Android = " "
AOSC = " "
Arch = " "
Artix = " "
CachyOS = " "
CentOS = " "
Debian = " "
DragonFly = " "
Elementary = " "
Emscripten = " "
EndeavourOS = " "
Fedora = " "
FreeBSD = " "
Garuda = "󰛓 "
Gentoo = " "
HardenedBSD = "󰞌 "
Illumos = "󰈸 "
Ios = "󰀷 "
Kali = " "
Linux = " "
Mabox = " "
Macos = " "
Manjaro = " "
Mariner = " "
MidnightBSD = " "
Mint = " "
NetBSD = " "
NixOS = " "
Nobara = " "
OpenBSD = "󰈺 "
openSUSE = " "
OracleLinux = "󰌷 "
Pop = " "
Raspbian = " "
Redhat = " "
RedHatEnterprise = " "
RockyLinux = " "
Redox = "󰀘 "
Solus = "󰠳 "
SUSE = " "
Ubuntu = " "
Unknown = " "
Void = " "
Windows = "󰍲 "
Zorin = " "
